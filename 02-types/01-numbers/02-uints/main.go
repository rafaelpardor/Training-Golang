package main

import (
	"fmt"
)

// The uint type is a value of integers, that only take positive numbers.
func main() {
	var c uint = 121
	// var err uint = -1

	fmt.Println(c)
	// fmt.Println(err)
}
