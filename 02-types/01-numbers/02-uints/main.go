package main

import (
	"fmt"
)

func main() {
	fmt.Println("The uint type is a value of integers, that only take positive numbers.")
	var c uint = 121
	// var err uint = -1

	fmt.Println(c)
	// fmt.Println(err)
}
